const PI = Math.PI;

// Define and export circleArea() and squareArea() below
circleArea= (radius) => PI * radius * radius;
module.export.circleArea;
module.export.squareArea= function(sideLength)
{
  return sideLength*sideLength;
};